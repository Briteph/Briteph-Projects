string formatDate_unixEpoch(string myDate, int addhours)
{
dateStr = myDate.replaceAll("T"," ").toDateTime().addBusinessHour(addhours).toDateTime().toString("dd-MM-yyyy HH:mm:ss","EST");
info dateStr;
info "LAst = " + dateStr.unixEpoch("EST");
return dateStr.unixEpoch("EST");
}
